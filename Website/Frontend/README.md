
  ## Running the code (Frontend)

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  ## Running the server (Backend)

  Run `uvicorn app:app --reload` to start server